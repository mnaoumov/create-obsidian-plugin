<%# The odu presets go through the dev-utils runner, which resolves `dprint.json` from the repo root -%>
<%# and falls back to the bundled one; standalone has no dev-utils and shells out directly. -%>
<%- render('preset') -%>
