<%# obsidian-dev-utils ships an esbuild build that already handles the dist folders, the manifest copy, -%>
<%# hot reload and the plugins-folder copy; standalone has no dev-utils and does all of that inline. -%>
<%- render('preset') -%>
