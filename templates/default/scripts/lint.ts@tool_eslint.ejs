<%# The obsidian-dev-utils presets go through the dev-utils runner, which finds the config and the -%>
<%# paths for them; standalone has no dev-utils and shells out directly. -%>
<%- render('preset') -%>
