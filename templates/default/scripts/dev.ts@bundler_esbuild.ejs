<%# obsidian-dev-utils ships an esbuild `dev()` that watches, restarts on node_modules changes and -%>
<%# re-runs build:compile; standalone has no dev-utils and its own build script is already the watcher. -%>
<%- render('preset') -%>
