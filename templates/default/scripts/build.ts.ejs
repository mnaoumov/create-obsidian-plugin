<%# Keyed on the BUNDLER first, as the lint and format scripts are keyed on their tool. Only esbuild -%>
<%# has a preset-specific build -- obsidian-dev-utils supplies one, and standalone inlines its own -- -%>
<%# so esbuild splits again underneath and the four CLI bundlers share one script. Splitting on the -%>
<%# preset first is what made every obsidian-dev-utils preset run esbuild whatever was chosen, while -%>
<%# still installing the chosen bundler and emitting its config for nothing to read. -%>
<%- render('bundler') -%>
