<%# Formatting is keyed on the TOOL first, unlike every other template here, which splits on the -%>
<%# preset. What `npm run format` runs is decided by the formatter answer; only dprint has a -%>
<%# preset-specific runner, so it splits again underneath. -%>
<%- render('tool') -%>
