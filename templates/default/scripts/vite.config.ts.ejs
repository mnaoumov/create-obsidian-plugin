import { builtinModules } from 'node:module';

import { defineConfig } from 'vite';
<%- render('import') -%>

const EXTERNAL = [
  'obsidian',
  'electron',
  '@codemirror/autocomplete',
  '@codemirror/collab',
  '@codemirror/commands',
  '@codemirror/language',
  '@codemirror/lint',
  '@codemirror/search',
  '@codemirror/state',
  '@codemirror/view',
  '@lezer/common',
  '@lezer/highlight',
  '@lezer/lr',
  'esbuild',
  'eslint',
  ...builtinModules,
];

export default defineConfig({
  plugins: [
<%- render('plugin') -%>
  ],
  build: {
    lib: {
      entry: 'src/main.ts',
      formats: ['cjs'],
      fileName: () => 'main.js',
    },
    rollupOptions: {
      external: EXTERNAL,
      output: {
        exports: 'default',
        banner: `/*
THIS IS A GENERATED/BUNDLED FILE BY VITE
if you want to view the source, please visit the github repository of this plugin
*/
`,
      },
    },
    sourcemap: process.env['BUILD'] !== 'production' ? 'inline' : false,
    minify: process.env['BUILD'] === 'production',
    outDir: '.',
    emptyOutDir: false,
  },
});
