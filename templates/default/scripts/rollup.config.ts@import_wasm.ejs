// eslint-disable-next-line import-x/no-rename-default -- Renamed on purpose: `asPluginFactory` below unwraps it, and the unwrapped binding is the `wasm` the plugin list calls.
import wasmModule from '@rollup/plugin-wasm';
