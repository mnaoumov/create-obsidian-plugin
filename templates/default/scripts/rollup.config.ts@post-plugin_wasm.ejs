    // `auto-inline` rather than the default `auto`: the default only inlines under `maxFileSize` and
    // Copies anything larger out to a separate file, which an Obsidian release does not ship. It also
    // Drops the `fs`/`path` branch the default emits for loading that separate file.
    wasm({ targetEnv: 'auto-inline' }),
