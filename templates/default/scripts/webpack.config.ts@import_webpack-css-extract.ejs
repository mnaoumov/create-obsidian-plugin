<%# Shared by EVERY styling answer that extracts CSS, rather than one identical copy per answer. -%>
<%# Partials are a Set, so a preset forcing a second styling answer on top of the chosen one -- which -%>
<%# `demo` does, with `scss` -- renders this once instead of emitting a duplicate `import` (TS2300). -%>
import MiniCssExtractPlugin from 'mini-css-extract-plugin';
