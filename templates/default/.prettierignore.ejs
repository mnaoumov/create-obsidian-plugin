dist
node_modules
*-lock.json

# The demo vault is documentation: its scripts show unusual module and `require` styles on purpose,
# and its `.obsidian` files are compared byte-for-byte by the demo-vault coverage suite.
demo-vault

# Markdown belongs to markdownlint.
*.md
