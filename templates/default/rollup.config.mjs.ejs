// This file is required by rollup at the root level.
// The actual configuration lives in scripts/rollup.config.ts
import { createJiti } from 'jiti';

const jiti = createJiti(import.meta.url);
const { config } = await jiti.import('./scripts/rollup.config.ts');
export default config;
