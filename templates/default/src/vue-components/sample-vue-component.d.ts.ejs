declare module '*.vue' {
  import type { DefineComponent } from 'vue';

  const component: DefineComponent<Record<string, unknown>, Record<string, unknown>, unknown>;
  // eslint-disable-next-line import-x/no-default-export -- A single-file component is imported as a default; the declaration has to match.
  export default component;
}
