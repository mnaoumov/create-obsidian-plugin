import { ItemView } from 'obsidian';

// eslint-disable-next-line import-x/no-unassigned-import -- Importing the module is what REGISTERS the custom element; there is nothing to bind.
import '../lit-elements/sample-lit-element.ts';

export const SAMPLE_LIT_VIEW_TYPE = '<%= pluginId %>-SampleLitView';

export class SampleLitView extends ItemView {
  public override getDisplayText(): string {
    return 'Sample Lit view';
  }

  public override getViewType(): string {
    return SAMPLE_LIT_VIEW_TYPE;
  }

  public override onClose(): Promise<void> {
    this.contentEl.empty();
    return super.onClose();
  }

  public override onOpen(): Promise<void> {
    const START_COUNT = 10;
    const element = this.contentEl.createEl('sample-lit-element');
    element.count = START_COUNT;
    element.vaultName = this.app.vault.getName();

    return super.onOpen();
  }
}
