.<%= pluginId %> {
  /* Add your plugin styles here */
}

/* The word-count widget the sample editor extension renders. Styled here rather than through
   `element.style`, so a theme can restyle it. */
.cm-sample-word-count {
  font-size: 0.85em;
  opacity: 0.5;
}
