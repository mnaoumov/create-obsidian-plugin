
## Sample WASM answer

A third command, which calls into the WebAssembly module the plugin ships. It is registered in `src/wasm/sample-command.ts` rather than `src/plugin.ts`, and [03 WebAssembly](<./03 WebAssembly.md>) walks through it.
