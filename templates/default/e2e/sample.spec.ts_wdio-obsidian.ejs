describe('<%= pluginName %>', () => {
  it('should load Obsidian', async () => {
    const title = await browser.getTitle();
    expect(title).toContain('Obsidian');
  });
<%- render('unofficial') -%>
});
