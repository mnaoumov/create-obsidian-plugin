// This file is nothing but such calls, so the ban on `void` is lifted for the whole of it rather than line
// By line.
/* eslint-disable no-void -- See above: no call in this file awaits its `node:test`, deliberately. */
