import { expect, test } from '@playwright/test';

test('<%= pluginName %> placeholder', async ({ page }) => {
  // Replace with actual Obsidian plugin E2E tests.
  // See https://playwright.dev/docs/writing-tests for guidance.
  expect(true).toBe(true);
});
